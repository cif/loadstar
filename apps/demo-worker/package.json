{
  "name": "demo-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate": "wrangler d1 execute loadstar-db --local --command \"SELECT 1\""
  },
  "dependencies": {
    "loadstar": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260527.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
